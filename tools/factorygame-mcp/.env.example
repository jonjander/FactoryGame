# Copy to `.env` in this folder (gitignored) if you want the MCP process to load secrets from a file.
# Optional: add `"envFile": "${workspaceFolder}/tools/factorygame-mcp/.env"` to a server entry in
# `.cursor/mcp.json`. Restart Cursor after edits.
#
# --- Azure dev (default in code if unset) ---
# FACTORYGAME_BASE_URL=https://factorygame-h5hmbzgncnazcmgu.swedencentral-01.azurewebsites.net
#
# --- Local API (http profile, avoids dev HTTPS cert issues in Node) ---
# FACTORYGAME_BASE_URL=http://localhost:5176
#
# FACTORYGAME_SESSION_TOKEN=
# FACTORYGAME_API_KEY=
# FACTORYGAME_ADMIN_TOKEN=
