# Copy to `.env` in this folder (gitignored) if you want the MCP process to load secrets from a file.
# Optional: add `"envFile": "${workspaceFolder}/tools/factorygame-mcp/.env"` to the `factorygame`
# server entry in `.cursor/mcp.json` (merge with existing keys). Restart Cursor after edits.
#
# FACTORYGAME_BASE_URL=https://factorygame-h5hmbzgncnazcmgu.swedencentral-01.azurewebsites.net
# FACTORYGAME_SESSION_TOKEN=
# FACTORYGAME_API_KEY=
# FACTORYGAME_ADMIN_TOKEN=
