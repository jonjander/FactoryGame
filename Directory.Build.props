<Project>
  <PropertyGroup>
    <!-- Bump this on each delivered release; referenced by UI, API metadata, and git tag v$(Version). -->
    <Version>0.3.12</Version>
    <IncludeSourceRevisionInInformationalVersion>false</IncludeSourceRevisionInInformationalVersion>
  </PropertyGroup>
</Project>
